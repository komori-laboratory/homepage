{
  "name": "komorilab-homepage",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:links": "node scripts/check-internal-links.mjs",
    "qa": "npm run build && npm run check && npm run check:links && node scripts/run-qa.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "chrome-launcher": "^1.2.1",
    "lighthouse": "^12.8.2",
    "playwright-core": "^1.58.2",
    "typescript": "^5.9.3"
  }
}
